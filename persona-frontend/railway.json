{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd persona-frontend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd persona-frontend && npm start",
    "healthcheckPath": "/api/test"
  }
}